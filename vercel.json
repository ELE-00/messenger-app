{
  "installCommand": "npm install",
  "buildCommand": "npm install vite --save-dev && npm run build",
  "outputDirectory": "dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
